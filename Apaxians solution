fullname = input()
new = ""
#iterating over each character in the string
for a in range(1,len(fullname)):
    #comparing character in string to the string preceding him and checking if there are not 2 characters similar
    if not(fullname[a])==fullname[a-1]:
        new+=fullname[a-1]
print(new+fullname[-1])
